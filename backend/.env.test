# Test Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=config_management_test

# Test Redis Configuration
REDIS_URL=redis://localhost:6380

# JWT (not critical for tests)
JWT_SECRET=test-secret-key
JWT_EXPIRATION=24h

# Server
PORT=3001
NODE_ENV=test
